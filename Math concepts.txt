import java.util.Random;
import java.util.Scanner;

public class ShoppingCart {
    public static void main(String[] args){
        System.out.println(Math.PI);
        System.out.println(Math.E);

        double result;

        result = Math.pow(2, 3);
        System.out.println(result);

        result = Math.abs(-45);
        System.out.println(result);

        result = Math.sqrt(81);
        System.out.println(result);

        result = Math.round(3.67);
        System.out.println(result);

        result = Math.ceil(4.45);
        System.out.println(result);

        result = Math.floor(4.55);
        System.out.println(result);

        result = Math.min(10, 56);
        System.out.println(result);

        result = Math.max(10, 34);
        System.out.println(result);

        double a;
        double b;
        double c;
        double radius;
        double circumference;
        double area;
        double volume;
        Scanner scanner = new Scanner(System.in);

        //hypotenuse

        System.out.print("Enter length of side A: ");
        a = scanner.nextDouble();

        System.out.print("Enter length of side B: ");5

        b = scanner.nextDouble();

        c = Math.sqrt(Math.pow(a ,2) + Math.pow(b ,2));
        System.out.println("Hypotenuse of side C is: " + c + "cm.");

        //circumference, area,

        System.out.print("Enter the radius of Cricle: ");
        radius = scanner.nextDouble();

        circumference = 2 * Math.PI * radius;
        area = Math.PI * Math.pow(radius, 2);
        volume = (4.0 / 3.0) * Math.PI * Math.pow(radius ,3);

        System.out.printf("Circumference of the Circle is: %.2f\n", circumference );
        System.out.printf("Area of Circle is: %.2f\n", area);
        System.out.printf("Volume of the circle is: %.2f\n", volume);


    }
}
