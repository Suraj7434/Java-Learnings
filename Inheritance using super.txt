Main.java************************************************

import java.io.CharArrayReader;

public class Main {
    public static void main(String[] args) {

            Person person = new Person("Suraj", "Phand");
            person.showName();

            Stud student = new Stud("Vaishnavi", "Phand", 8.12);
            student.showGpa();

            emp employee = new emp("Namdeo Phand", "Phand", 120000);
            employee.showSalary();
        }
    }

Person.java(parent class)*********************************

public class Person {

    String first;
    String last;

    Person(String first, String last){
        this.first = first;
        this.last = last;
    }

    void showName(){
        System.out.println(this.first + " " + this.last);
    }
}

Stud.java(child class)************************************

public class Stud extends Person{

    double gpa;

    Stud(String first, String last, double gpa){
        super(first, last);
        this.gpa = gpa;
    }

    void showGpa(){
        System.out.println(this.first + "'s GPA is: " + gpa);
    }
}

emp.java(child class)*************************************

public class emp extends Person{

    int salary;

    emp(String first, String last, int salary){
        super(first, last);
        this.salary = salary;
    }

    void showSalary(){
        System.out.println(this.first + "'s salary is: " + salary);
    }
}
